/**
 * @file bookmark.ts
 * @description Defines the Bookmark interface for the application.
 */

/**
 * Interface for a Bookmark object.
 * Represents a single bookmark with a URL and a title.
 */
export interface Bookmark {
  id: string;
  url: string;
  title: string;
}
